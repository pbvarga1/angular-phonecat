version: '3.0'
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8000:8000"
        volumes:
            - "./:/var/www"
    test:
        build:
            context: tests/bdd
            dockerfile: Dockerfile
        depends_on:
            - "app"
        volumes:
            - "./tests/bdd:/tests"
